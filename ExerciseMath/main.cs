using System;
using static System.Console;

static class main{
	double x=1;
	double times2(double y){
		double z=7;
		WriteLine(x);
		return y*2
	}
	static int Main(){
		Write($"x={x}"\n);
		return 0;
	}
}

